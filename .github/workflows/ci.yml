name: CI

on:
  push:
    branches: ["**"]
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    runs-on: ubuntu-latest

    services:
      db:
        # PostGIS, weil ihr PostGIS im Canvas/Stack habt
        image: postgis/postgis:15-3.4
        env:
          POSTGRES_USER: ghcn
          POSTGRES_PASSWORD: ghcn
          POSTGRES_DB: ghcn
        options: >-
          --health-cmd "pg_isready -U ghcn -d ghcn"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
        ports:
          - 5432:5432

    env:
      # Prisma braucht DATABASE_URL im Prozess (Vitest lädt keine .env in CI)
      # localhost ist ok, weil wir den DB-Service-Port auf den Runner mappen
      DATABASE_URL: postgresql://ghcn:ghcn@localhost:5432/ghcn?schema=public

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      # Wichtig: KEINE Version hier angeben, wenn im package.json bereits "packageManager": "pnpm@x.y.z" steht.
      # Sonst: "Multiple versions of pnpm specified"
      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Get pnpm store path
        id: pnpm-cache
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Prisma migrate + seed
        run: |
          pnpm prisma migrate deploy
          pnpm prisma db seed

      - name: Lint
        run: pnpm lint

      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build
